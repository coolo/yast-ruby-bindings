#!/usr/bin/make -f

export DH_VERBOSE=1

%:
	dh $@

# use lib instead of lib64
override_dh_auto_configure:
	dh_auto_configure -- -DLIB=lib

# disable tests, rspec is missing at build service :-(
override_dh_auto_test:

